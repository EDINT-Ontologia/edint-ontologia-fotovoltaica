# FOT11: ¿Cuál es el periodo temporal asociado a cada observación de generación fotovoltaica?

PREFIX sosa:     <http://www.w3.org/ns/sosa/>
PREFIX edintfot: <https://edint.es/def/fotovoltaica#>

SELECT ?observation ?timeEntity
WHERE {
  ?observation a edintfot:PhotovoltaicGeneration ;
               sosa:phenomenonTime ?timeEntity .
}
ORDER BY ?observation
